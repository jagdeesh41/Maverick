# Copy this file to .env and adjust if your backend runs somewhere else.
# This is the ONLY place the backend URL is configured — every API call
# in src/api.js reads from this one value.
VITE_API_BASE_URL=http://localhost:8080
